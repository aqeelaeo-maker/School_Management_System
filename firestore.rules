rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Multi-School Management System Rules
    // Allow read and write for schools, students, classes, attendance, fees, exams, books, routes, etc.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
